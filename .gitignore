node_modules
.turbo
.next
dist
build
.env
.env.local
.env.*
!.env.example
coverage
playwright-report
test-results
*.log
data
tmp
.pnpm-store/*
.cursor/

# Local strategy / ICP and contributor notes for Cursor (not published).
AGENTS.md
agents.md

# PGlite can create this literal directory name if :memory: is resolved as a relative path (e.g. wrong cwd).
packages/db/:memory:/
.tmp/*

# Generated from repo root README before `npm pack` / `npm publish` (packages/cli).
packages/cli/README.md
